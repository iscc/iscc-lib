# Handoff

## 2026-07-28 — Pin the .NET test dependencies and commit a NuGet lock file

**Done:** Pinned the three floating test package refs in `Iscc.Lib.Tests.csproj` to their
restore-resolved versions (`Microsoft.NET.Test.Sdk` 18.8.1, `xunit.v3` 3.2.2,
`xunit.runner.visualstudio` 3.1.5), committed the restore-generated `packages.lock.json`, and made
the CI `dotnet` job restore in `--locked-mode` with `--no-restore` on build and test. Fixed the
stale Ruby Magnus doc line to teach `ruby.str_from_slice(&bytes)`.

**Files changed:**

- `packages/dotnet/Iscc.Lib.Tests/Iscc.Lib.Tests.csproj`: `RestorePackagesWithLockFile` property +
    exact `Version=` pins (derived from restore output, not retyped — resolution matched next.md's
    sanity check exactly)
- `packages/dotnet/Iscc.Lib.Tests/packages.lock.json`: new, tracked (generated by
    `dotnet restore --force-evaluate` after pinning; `requested` ranges are `[18.8.1, )` etc.)
- `.github/workflows/ci.yml`: `dotnet` job gains a `Restore .NET packages (locked mode)` step; build
    and test steps carry `--no-restore`; `-e LD_LIBRARY_PATH` form untouched
- `packages/dotnet/CLAUDE.md`: file-layout entry for the lock file, pitfall bullet on regenerating
    it with `--force-evaluate` on any version bump
- `crates/iscc-rb/CLAUDE.md`: line 108 now teaches `ruby.str_from_slice(&bytes)` via `Ruby::get()`

**Verification:** (all six Done-When criteria pass)

- `grep -E 'Version="[^"]*\*"'` on the test csproj → exit 1, no floating range survives
- `git ls-files --error-unmatch packages/dotnet/Iscc.Lib.Tests/packages.lock.json` → exit 0; lock
    `resolved` == csproj `Version` for all three direct refs (checked by script, printed both)
- `dotnet restore … --locked-mode` → exit 0, `git status --porcelain packages/dotnet` shows only the
    staged index entries, zero worktree modifications (restore rewrites nothing)
- `cargo build -p iscc-ffi` then `dotnet test packages/dotnet/Iscc.Lib.Tests/ -e LD_LIBRARY_PATH=…`
    → **Failed: 0, Passed: 104, Total: 104** — the exact decisions.md 2026-07-27 count
- `grep -n "RString::from_slice" crates/iscc-rb/CLAUDE.md` → exit 1; `str_from_slice` present (1)
- `mise run check` → exit 0 (incl. CI-job-table and docs-nav parity hooks); tracked tree unchanged

**False-green proof (required by next.md):** in a throwaway tree (`git archive HEAD | tar -x` +
working-tree csproj/lock copied in), bumping `xunit.v3` 3.2.2 → 3.2.1 made
`dotnet restore --locked-mode` exit **1** with
`NU1004: The package reference xunit.v3 version has changed from [3.2.2, ) to [3.2.1, )`. Exit code
checked via file redirect, not a pipe.

**Locked-mode/Iscc.Lib note:** `--locked-mode` accepted the referenced `Iscc.Lib` project without a
lock file (it declares zero `PackageReference`s) — the contingency file next.md allowed was not
needed. Also probed: prek's `end-of-file-fixer` adds a trailing newline to the lock file, and
neither `dotnet restore --locked-mode` nor plain `dotnet build` rewrites it afterward (md5 stable
across both) — no formatter/restore tug-of-war.

**Action-freshness evidence (for review to retire that bullet of the dependency issue):** re-ran the
loop 2026-07-28 over all 25 distinct `uses:` refs in `.github/workflows/*.yml` (23 versioned +
`dtolnay/rust-toolchain@stable` and `pypa/gh-action-pypi-publish@release/v1`, branch-pinned by
design). `gh api repos/<o>/<r>/git/matching-refs/tags/v<N+1>` returned **0 tags for every one of the
23** (incl. `github/codeql-action` v5 and `oxidize-rb/actions` v2), and the two exact pins are their
publisher's `releases/latest`: `astral-sh/setup-uv` → v9.0.0,
`rubygems/configure-rubygems-credentials` → v2.1.0. The release.yml action-freshness pass is a
confirmed no-op; nothing to bump.

**Next:** With this landed, the CID-doable slice of the v0.6.0 dependency issue looks exhausted
(uniffi 0.32 and criterion 0.8 stay human-gated; action majors are all current as of today). The
remaining doc-drift half (`specs/java-bindings.md`) is human-gated. Worth checking whether any
`normal` issue remains schedulable at all — this may be approaching HUMAN REVIEW / IDLE territory.

**Notes:**

- The CI `dotnet` job's name and the job set are unchanged, so the `check-ci-job-table` parity gate
    needed no spec edit (it gates job keys/names, not steps) — and `mise run check` confirms.
- `dotnet test … --no-restore` still compiles (it is `--no-build` that would skip compilation); the
    locked restore is now the only restore in the job.
- NuGet emits `packages.lock.json` without a trailing newline; the committed copy carries the
    prek-normalized trailing newline and is stable under restore (verified, see above).
- Magnus Patterns list in `crates/iscc-rb/CLAUDE.md` audited against `src/lib.rs` as instructed:
    `#[magnus::wrap]`, `RefCell<Option<…>>`, `_`-prefix registration names, and
    `unsafe { data.as_slice() }` all still accurate; only the `from_slice` bullet was stale.
