# Handoff

## 2026-07-25 — Dependency refresh slice 6 — Go module (`go.mod`/`go.sum`) + stale Kotlin README version target

**Done:** Refreshed the Go package's third-party modules via
`go get -C packages/go -u ./... && go mod tidy -C packages/go` (`golang.org/x/text` 0.34.0 → 0.40.0
direct, `github.com/klauspost/cpuid/v2` 2.0.12 → 2.4.0 indirect; no hold-back needed — all
conformance tests pass on the refreshed Unicode tables). Added `packages/kotlin/README.md` to
`scripts/version_sync.py` `TARGETS` and let `mise run version:sync` rewrite the stale
`io.iscc:iscc-lib-kotlin:0.3.1` → `0.5.0` (proving the wiring; no hand-edit).

**Files changed:**

- `packages/go/go.mod`: x/text v0.40.0, cpuid/v2 v2.4.0; new indirect `golang.org/x/sys v0.47.0`
    (required by cpuid/v2 2.4.0 — see Notes); `go 1.26.1` floor untouched, no `toolchain` line,
    blake3 unchanged at v0.2.4
- `packages/go/go.sum`: regenerated by `go mod tidy` (not hand-edited)
- `scripts/version_sync.py`: one `TARGETS` entry appended —
    `("packages/kotlin/README.md", _get_doc_dep_version, _sync_doc_deps)` (existing `GRADLE_DEP_RE`
    matches; no regex change)
- `packages/kotlin/README.md`: line 13 `0.3.1` → `0.5.0`, written by `mise run version:sync`

**Verification:** (every command run this session)

- [x] `CGO_ENABLED=0 go test -C packages/go -count=1 ./...` → exit 0
    (`ok github.com/iscc/iscc-lib/packages/go`, includes conformance vectors — the x/text Unicode
    table risk did not materialize)
- [x] `go vet -C packages/go ./...` → exit 0
- [x] `go mod tidy -C packages/go -diff` → exit 0 (module graph tidy)
- [x] `grep 'golang.org/x/text v0\.40\.0' packages/go/go.mod` → match (no hold-back needed)
- [x] `grep 'github.com/klauspost/cpuid/v2 v2\.4\.0' packages/go/go.mod` → match
- [x] `grep 'github.com/zeebo/blake3 v0\.2\.4' packages/go/go.mod` → match (unchanged)
- [x] `grep -c '^go 1\.26\.1$' packages/go/go.mod` → 1; `grep -c '^toolchain'` → 0
- [x] `grep 'packages/kotlin/README.md' scripts/version_sync.py` → match
- [x] `mise run version:check` → exit 0, output contains `OK: packages/kotlin/README.md = 0.5.0`
    (all 21 targets OK)
- [x] `grep -rn '0\.3\.1' packages/kotlin/README.md` → exit 1 (no matches)
- [x] `mise run check` → exit 0, all 15 hooks passed, nothing rewritten (ran `mise run format`
    first)
- [x] `git status --porcelain crates/ .crap-baseline.json` → empty; committed diff touches only
    `packages/go/go.{mod,sum}`, `packages/kotlin/README.md`, `scripts/version_sync.py` (+ handoff,
    memory)

**napi/dotnet closure (re-checked this session, per next.md — review agent may strike from the slice
ledger):**

- `crates/iscc-napi/package.json` devDependencies: `"@napi-rs/cli": "^3"` — floats over the 3.x
    line, covers latest 3.7.4. Current; no edit.
- `packages/dotnet/Iscc.Lib.Tests/Iscc.Lib.Tests.csproj`: `Microsoft.NET.Test.Sdk 17.*`,
    `xunit 2.*`, `xunit.runner.visualstudio 2.*` — wildcard floats, current within their major lines
    (xunit 3.x / Test.Sdk 18.x majors remain their own future slices). No edit.

**Next:** Remaining refresh slices: Ruby manifests (`Gemfile`/gemspec, bundle with the magnus 0.8
question), `release.yml` action refs (97 `uses:`, only truly validated by a release run), ruff 0.16
adoption, and the deferred majors (xunit 3, Test.Sdk 18, Gradle wrapper 9, JUnit 6). Alternatively
the Kotlin consumer-floor decision is still parked on HUMAN REVIEW.

**Notes:**

- **One un-anticipated but mechanical diff line:** `golang.org/x/sys v0.47.0` appeared as a new
    indirect requirement. cpuid/v2 2.4.0 requires x/sys (for CPU feature detection syscalls);
    `go mod tidy` added it and `go mod tidy -diff` confirms it is required, not cruft. x/sys v0.47.0
    declares `go 1.24.0` — below the 1.26.1 floor, so no directive pressure. next.md's expected
    go.mod listed only the two bumps, but this is a direct consequence of the cpuid bump, not scope
    creep.
- `go get -u` briefly downloaded `klauspost/cpuid` v1 (module-graph probing); it does NOT appear in
    go.mod or go.sum.
- No `// held:` comment was needed anywhere — nothing was held back in this slice.
- The version_sync module docstring's generic line "Maven/Gradle/SPM version snippets in docs and
    READMEs" already covers the Kotlin README; no docstring change needed.
- `iterations.jsonl` is runner-owned and left unstaged as usual.
