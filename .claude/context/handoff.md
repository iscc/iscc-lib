# Handoff

## 2026-07-24 — Refresh Python `uv.lock` (uv lock --upgrade) — dependency-refresh slice 2

**Done:** Ran `uv lock --upgrade` at the repo root, refreshing 40 packages in `uv.lock` to their
latest compatible releases (notable: iscc-core 1.2.2→1.3.0, ty 0.0.18→0.0.63, zensical
0.0.23→0.0.51, prek 0.3.3→0.4.11, pytest 9.0.2→9.1.1, maturin 1.12.4→1.14.1). One hold-back was
required: ruff 0.16.0 expands its default lint rule set and produced 104 new `ruff check` errors
across 13 files (72 in `crates/iscc-py/python/iscc_lib/_lowlevel.pyi` — rules PIE790, PYI048,
RUF100, I001, RUF059, EXE001, PLW1510, RUF022), so per the work package's stated priority for a
lockfile slice, `ruff<0.16` was pinned in `pyproject.toml` with an inline hold-back comment and the
lock re-resolved to ruff 0.15.22 (still a patch bump from 0.15.2). No source files were touched.

**Files changed:**

- `uv.lock`: regenerated by `uv lock --upgrade` (40 packages bumped; ruff re-resolved to 0.15.22
    after the hold-back)
- `pyproject.toml`: single dev-group pin `ruff<0.16` with hold-back comment (taplo auto-aligned the
    comment column — cosmetic)

**Verification:** (all run this session on the final refreshed lock + rebuilt extension)

- `uv lock --check` exits 0 ("Resolved 91 packages") — committed lock consistent with
    `pyproject.toml`.
- `uv sync --group dev` exits 0; `uv run maturin develop --manifest-path crates/iscc-py/Cargo.toml`
    (maturin 1.14.1) builds and installs the abi3 extension.
- `mise run test` passes — Rust workspace tests green, Python 314 passed (incl. conformance vs
    `data.json` and the iscc-core comparative benches now against iscc-core **1.3.0**).
- `mise run lint` clean — `ruff check` "All checks passed!", `ruff format --check` "24 files already
    formatted", cargo fmt/clippy unchanged.
- `mise run check` — all 15 pre-commit hooks pass (first run had taplo reformat my pyproject
    comment; second run fully green, no rewrites left in tree).
- Pre-push stage hooks (`uv run prek run --all-files --hook-stage pre-push`) all pass — notably
    "Python type checking" is green on **ty 0.0.63** (no new type errors from the 0.0.18→0.0.63
    jump) and Rust/Python test hooks pass under prek 0.4.11.
- `uv run zensical build` exits 0 (zensical 0.0.51) and `uv run python scripts/gen_llms_full.py`
    exits 0 (22 pages, 268195 bytes).
- Working tree diff is `uv.lock` + `pyproject.toml` only (plus this handoff + advance memory);
    `crates/iscc-py/uv.lock` untouched; no Rust or Python source file modified.

**Next:** Continue the dependency-refresh issue with the next slice — either (a) Rust direct-pin
evaluation (document hold-back reasons next to each held major in `Cargo.toml`) or (b) a per-binding
manifest slice (npm/gems/maven/gradle/go.mod). Separately, a small dedicated step should adopt ruff
0.16: run `ruff check --fix` (60 auto-fixable), hand-fix the rest (mostly `_lowlevel.pyi` stub-style
PIE790/PYI048), and drop the `ruff<0.16` pin.

**Notes:**

- **ruff 0.16 hold-back is deliberate scope discipline**, not gate weakening: next.md's
    implementation notes explicitly prefer "a single documented hold-back over source churn so the
    diff stays a lockfile refresh". No lint rule was disabled; ruff 0.15.22 enforces exactly the
    same rule set as the previously locked 0.15.2.
- **Out-of-scope doc defect found:** zensical 0.0.51 has an anchor checker; it warns (non-fatal,
    exit 0) that `docs/howto/c-cpp.md:9` links to `#c-wrapper-iscc-hpp`, which does not exist in
    that page (no heading slugs to it). Docs CI (`docs.yml` runs plain `zensical build`, non-strict)
    stays green, but the intra-page link is genuinely broken and worth a small follow-up fix.
- **Gotcha for future refreshes:** `uv sync` uninstalls the editable `iscc-lib` extension package —
    `uv run maturin develop` must be re-run after every sync or pytest import fails.
- iscc-core 1.3.0 matches the vendored conformance vectors version (`data.json` v1.3.0); the
    comparative tests confirm no output drift.
- No `decisions.md` entry needed beyond the handoff: the ruff hold-back follows the exact
    contingency next.md pre-authorized.
