# Handoff

## 2026-07-24 — Refresh Rust `Cargo.lock` (cargo update) — dependency-refresh slice 1 (Rust ecosystem)

**Done:** Ran `cargo update` with no `-p` filter, regenerating `Cargo.lock` within the existing
`Cargo.toml` caret constraints. ~100 crates moved to latest semver-compatible releases (net −176
lockfile lines: 289 insertions, 465 deletions — the napi `wit-bindgen`/`wasm-tools`/`wasmparser`
family dropped out entirely). All four required gates verified green on the refreshed lockfile. No
source, docs, or manifest files changed.

**Files changed:**

- `Cargo.lock`: regenerated by `cargo update`. Key moves (all within pins): uniffi 0.31.0 → 0.31.2
    (NOT 0.32 — pin honored), blake3 1.8.3 → 1.8.5, xxhash-rust 0.8.15 → 0.8.18, serde 1.0.228 →
    1.0.229, serde_json 1.0.149 → 1.0.151, wasm-bindgen 0.2.125 → 0.2.126, thiserror 2.0.18 →
    2.0.19, tempfile 3.26.0 → 3.27.0, zerocopy 0.8.39 → 0.8.55, regex-syntax 0.8.9 → 0.8.11, plus
    transitive additions `syn v3.0.3` (via serde_derive/async-trait proc-macros) and `shlex v2.0.1`
    (via `cc`, blake3 build-dep) — all license-clean per cargo-deny.

**Verification:** (all from this session's tool outputs)

- `git diff --name-only` shows only `Cargo.lock` (+ runner-managed `iterations.jsonl`, unstaged);
    `git diff --name-only -- Cargo.toml` is empty — pins untouched.
- uniffi pin intact: `Cargo.toml:47` is `uniffi = "0.31"`. NOTE: next.md's literal grep
    `'uniffi = { version = "0.31"'` fails because the pin uses the plain-string form, not an inline
    table — the intent (pin unchanged, lockfile at 0.31.2 not 0.32.0) is satisfied and confirmed via
    `grep -n 'uniffi' Cargo.toml` + the `cargo update` output line
    `Updating uniffi v0.31.0 -> v0.31.2 (available: v0.32.0)`.
- `mise run test` — exit 0. Rust: 427 tests across suites (270+85+28+22+21+1), 0 failed. Python: 314
    passed (incl. conformance + comparative benches vs iscc-core).
- `mise run lint` — exit 0 (`cargo fmt --check`, clippy `-D warnings` across the workspace incl. all
    binding crates, ruff; "All checks passed!").
- `mise run audit` — exit 0: "advisories ok, bans ok, licenses ok, sources ok" on the refreshed
    lockfile. No deny.toml changes needed.
- `mise run bench:iai:check` — exit 0: "16 benchmark(s) within 10% of baseline (Ir)". Max drift
    +1.96% (`bench_mixed_code.two_codes`), most ±0.1%; blake3 1.8.5 did not shift hashing Ir counts.
    `.iai-baseline.json` NOT refreshed — unnecessary.

**Next:** Per next.md's Not-In-Scope carve-outs, the follow-up slices of the dependency-refresh
issue: "Rust direct-pin evaluation" (document hold-back reasons for uniffi 0.32, pyo3 #41, criterion
0.7?, iai-callgrind, magnus, jni, napi), then Python `uv.lock`, then the per-binding manifest groups
and tooling pins.

**Notes:**

- **Environment gap (fixed in-session, worth a devcontainer follow-up):** this container had NEITHER
    `valgrind` nor `iai-callgrind-runner` installed, so the first `mise run bench:iai:check` failed
    with "No such file or directory". Installed via `sudo apt-get install -y valgrind` (3.19.0) and
    `cargo binstall -y iai-callgrind-runner --version 0.16.1`, then the gate ran and passed. These
    installs are container-local (not in the repo); if the devcontainer setup is supposed to provide
    them, that provisioning step is missing/broken — possible small issue for issues.md (reviewer's
    call).
- **Pre-existing, not from this diff:** cargo emits a future-incompat warning for
    `proc-macro-error2 v2.0.1` (dev-dep chain: iai-callgrind-macros 0.6.1 → iai-callgrind 0.16,
    benches only). It was already at 2.0.1 before this refresh (lockfile diff shows no version
    change). An `iai-callgrind` bump in the "direct-pin evaluation" follow-up would address it.
- cargo reports 3 dependencies intentionally behind latest (the held pins: uniffi 0.32, etc.) —
    exactly the carve-outs next.md defers to the direct-pin evaluation step.
- CI will re-run the same gates (deny, perf vs `.iai-baseline.json`, full matrix) on push; local
    runs used the same tool versions (cargo-deny 0.19.9, iai-callgrind 0.16.1).
