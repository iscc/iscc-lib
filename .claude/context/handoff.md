## 2026-03-02 — Review of: Expose `add_units`/`units` in Node.js binding

**Verdict:** PASS

**Summary:** Clean, correctly scoped implementation exposing `add_units: Option<bool>` parameter and
`units: Option<Vec<String>>` field in the Node.js NAPI binding. The code follows the established
`Option<T>` pattern for optional JS parameters exactly. Three well-targeted tests cover
enabled/disabled/content verification. All 135 tests pass, all quality gates green.

**Verification:**

- [x] `cargo build -p iscc-napi` compiles without errors
- [x] `cargo clippy -p iscc-napi -- -D warnings` — clean
- [x] `cd crates/iscc-napi && npm run build && npm test` — 135 tests pass (132 existing + 3 new)
- [x] `gen_sum_code_v0(path, 64, false, true)` returns object with `units` array of 2 ISCC strings —
    confirmed via test
- [x] `gen_sum_code_v0(path)` returns object with `units === undefined` — confirmed via test
- [x] `mise run check` — 14/14 hooks pass

**Issues found:**

- (none)

**Codex review:** Codex confirmed the change is backward-compatible and compiles clean. It verified
`index.d.ts` (auto-generated) correctly includes `units?: Array<string>` in `NapiSumCodeResult` and
the `addUnits` optional parameter. No findings.

**Next:** Continue binding propagation for issue #21. Remaining bindings: WASM (`crates/iscc-wasm`),
C FFI (`crates/iscc-ffi`), JNI (`crates/iscc-jni`), Go (`packages/go`). WASM is a good next target
since it uses a similar pattern to NAPI (struct + function wrapper). The WASM binding has its own
inline `gen_sum_code_v0` implementation (does not call `iscc_lib::gen_sum_code_v0`) — the advance
agent needs to add `add_units` support to the inline implementation, not just pass-through.

**Notes:** Node.js binding propagation was straightforward — the `#[napi(object)]` derive handles
`Option<Vec<String>>` as `string[] | undefined` automatically, and `Option<bool>` maps to optional
JS parameter. No `.pyi` stub equivalent exists for NAPI (types are auto-generated by `napi build`).
Python test count: 207, Node.js test count: 135. Four bindings remain for issue #21.
