# Handoff

## 2026-07-27 — Bump the Kotlin package's Gradle wrapper from 8.12.1 to 9.6.1

**Done:** Two-pass wrapper upgrade (`gradlew -p packages/kotlin wrapper --gradle-version 9.6.1` run
twice, so the jar and scripts are regenerated *by* 9.6.1), restated the `junit-platform-launcher`
comment for the Gradle 9 reality (explicit launcher now *required*, not a workaround for 8.12.1's
stale injected launcher), and fixed the stale `Gradle 8.x` prerequisite in the package CLAUDE.md.

**Files changed:**

- `packages/kotlin/gradle/wrapper/gradle-wrapper.properties`: distributionUrl →
    `gradle-9.6.1-bin.zip`; Gradle 9 wrapper adds default `retries=0` / `retryBackOffMs=500` keys
    (tool-generated)
- `packages/kotlin/gradle/wrapper/gradle-wrapper.jar`: regenerated by 9.6.1 (43583 → 48462 bytes)
- `packages/kotlin/gradlew`, `packages/kotlin/gradlew.bat`: regenerated by 9.6.1; `.bat` came out
    CRLF and was normalized to LF by `mise run format` (mixed-line-ending hook); `gradlew` index
    mode still 100755
- `packages/kotlin/build.gradle.kts`: only the comment above
    `testRuntimeOnly(junit-platform-launcher:1.14.4)` — dependency itself and the `// held:` JUnit
    6.x note untouched
- `packages/kotlin/CLAUDE.md`: prerequisite line now `Gradle 9.x (wrapper checked in)`

**Verification:**

- [x] `gradle-wrapper.properties` contains `gradle-9.6.1-bin.zip`;
    `gradlew -p packages/kotlin --version` reports `Gradle 9.6.1` (launcher JVM 17.0.19)
- [x] `cargo build -p iscc-uniffi` (exit 0) then `gradlew -p packages/kotlin clean build` — exit 0,
    `BUILD SUCCESSFUL`, 8 actionable tasks executed
- [x] `TEST-uniffi.iscc_uniffi.ConformanceTest.xml`: `tests="9" failures="0" errors="0"`;
    `...UnicodeBoundaryTest.xml`: `tests="13" failures="0" errors="0"` — totals unchanged
- [x] `grep -n "Deprecated" /tmp/gradle-build.log` → zero matches: no KGP
    `Deprecated Gradle Version` diagnostic (it *was* printed during the first wrapper pass, still
    under 8.12.1, confirming 9.6.1 > 8.14.4 is what retires it) and no residual
    `Deprecated Gradle features` notice either
- [x] `grep -rn "Gradle 8" packages/kotlin/CLAUDE.md packages/kotlin/build.gradle.kts` → no match
- [x] `mise run check` — exit 0, no file modified
- [x] Optional provenance: Gradle 9 deletes the distribution zip after extraction (only `.ok` marker
    remains in `~/.gradle/wrapper/dists`), so I hashed a fresh download of the same URL: sha256
    `9c0f7faeeb306cb14e4279a3e084ca6b596894089a0638e68a07c945a32c9e14` — exact match with next.md's
    expected value

**Next:** The JUnit 6.x migration is now unblocked on a supported Gradle base — it spans
`packages/kotlin/build.gradle.kts` and `crates/iscc-jni/java/pom.xml` and renumbers the platform
artifacts 1.x→6.x. Under Gradle 9 any launcher-injection failure in that step will be attributable
to JUnit alone.

**Notes:**

- **Slice ledger for review to apply to issues.md**: "Dependency review and refresh" — the Gradle
    wrapper major (8.12.1 → 9.6.1) is done as of this iteration. Remaining bullets: JUnit 6.x
    migration, `jni` 0.22, `magnus` 0.8, `release.yml` actions.
- Not-In-Scope held: KGP stays `2.4.10`, `pom.xml` byte-untouched, no `distributionSha256Sum`, no
    wrapper-validation CI step, no configuration cache, no workflow/java-version edits.
- Gradle 9.6.1 prints two benign non-deprecation notices: an `[Incubating] Problems report` line and
    a "Consider enabling configuration cache" suggestion. Config cache is explicitly out of scope;
    if it's ever wanted it is its own step.
- No Rust source, public API, or benchmarked hot path touched — CRAP/iai baselines correctly
    unmoved.
