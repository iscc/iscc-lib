## 2026-07-29 — Review of: Mint `gen_iscc_id_v1` on WASM + settle JS-number input validation

**Verdict:** PASS

**Summary:** The advance agent added `gen_iscc_id_v1` to `crates/iscc-wasm` (bare-string, 33 Tier 1
symbols) and retrofitted the same `f64` + `checked()` input validation onto napi, so both JS-number
surfaces now reject non-finite / non-integral / negative / out-of-range inputs before narrowing.
Diff is scope-clean (2 non-test source files), all gates green, the golden vector holds, and the
`normal` napi coercion issue is fully resolved on both surfaces. Codex found nothing.

**Verification:**

- [x] `wasm-pack test --node crates/iscc-wasm --features conformance --test unit` — **84 pass / 0
    fail**, incl. golden `ISCC:MAIGHFECJMOPMIAB`, decode round-trip (maintype 6 / version 1 /
    subtype 0), and negative / non-integral / non-finite / out-of-range throws.
- [x] `cd crates/iscc-napi && npm run build:debug && npm run test` — **162 pass / 0 fail** (was
    158). `index.d.ts:150` regenerated: `gen_iscc_id_v1(timestamp, hubId, realm): string`.
- [x] `cargo clippy -p iscc-wasm -p iscc-napi --all-targets -- -D warnings` — clean.
- [x] `cargo fmt -p iscc-wasm -p iscc-napi --check` — clean.
- [x] `gen_iscc_id_v1` exported on wasm (33 symbols); both surfaces throw on invalid input —
    asserted by the new tests on both.
- [x] `mise run check` — all prek pre-commit hooks Passed.

**Issues found:**

- (none blocking) Resolved and deleted the `normal` JS-number coercion issue; updated #43 to show
    napi/wasm minting done. Recorded the `f64`-vs-typed-int design call in decisions.md so the
    remaining fan-out surfaces don't re-litigate it.

**Codex review:** Clean — confirmed the WASM export delegates correctly to core, both JS surfaces
validate before narrowing, and the added tests cover output / round-trip / invalid-input without
regressions. No findings.

**Next:** #43 fan-out continues on the typed-int surfaces (no JS-coercion hazard, no `checked()`
pattern — core re-checks ranges): ffi first (needs `iscc.h` regen + a freshness gate), then jni, rb,
uniffi→Swift/Kotlin, dotnet, cpp — one per step. After the fan-out, the Tier-1 32→33 doc/count sweep
across the stale sites named in #43 (wasm CLAUDE.md still reads "30 Tier 1"; core CLAUDE.md "32").

**Notes:** `checked()` is deliberately duplicated across the two files (~5 lines each) — sanctioned
by next.md over a shared crate. napi signature change `(f64,u16,u8)`→`(f64,f64,f64)` is
`develop`-only (experimental fn, unreleased) — no API-break. `index.js`/`index.d.ts`/`*.node` are
gitignored artifacts regenerated by the napi build. learnings.md at 200; decisions.md at 404 (runner
rotates).
