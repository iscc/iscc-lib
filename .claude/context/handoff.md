# Handoff

## 2026-07-25 — Dependency refresh slice 7 — Ruby manifests (`Gemfile` + `Gemfile.lock`)

**Done:** Pinned `rb_sys` exactly at `0.9.123` in `crates/iscc-rb/Gemfile` with a `# held:` comment
explaining the `oxidize-rb/actions/cross-gem` Docker-image coupling, added a second `# held:`
comment documenting the `minitest` 5.x hold (6.x requires Ruby >= 3.2), then ran `bundle update` to
refresh all other dev dependencies to their newest Ruby-3.1-installable versions. Synced the "rb_sys
Version Pinning" section of `crates/iscc-rb/CLAUDE.md` to state the pin now lives in both Gemfile
and Gemfile.lock and must move together with `tag:` in `release.yml`.

**Files changed:**

- `crates/iscc-rb/Gemfile`: `gem "rb_sys", "~> 0.9"` → `gem "rb_sys", "0.9.123"` + two `# held:`
    comments (rb_sys, minitest)
- `crates/iscc-rb/Gemfile.lock`: regenerated by `bundle update` — rake 13.3.1→13.4.2, standard
    1.54.0→1.56.0, rubocop 1.84.2→1.88.2, rubocop-minitest 0.39.1→0.40.0, json 2.18.1→2.21.1, parser
    3.3.10.2→3.3.12.0, rubocop-ast 1.49.0→1.50.0, regexp_parser 2.11.3→2.12.0, parallel
    1.27.0→1.28.0, language_server-protocol 3.17.0.5→3.17.0.6; DEPENDENCIES now `rb_sys (= 0.9.123)`
- `crates/iscc-rb/CLAUDE.md`: "rb_sys Version Pinning" intro updated (pin in both files, change
    together with `release.yml` `tag:`)

**Verification:** (all run this session, exit codes observed)

- `(cd crates/iscc-rb && bundle exec standardrb)` — exit 0, **zero lint fallout** from standard 1.56
    / rubocop 1.88, so no Ruby source file was touched (the optional 4th file was not needed)
- `(cd crates/iscc-rb && bundle exec rake compile)` — exit 0
- `(cd crates/iscc-rb && bundle exec rake test)` — exit 0,
    `111 runs, 299 assertions, 0 failures,   0 errors, 0 skips` (matches the pre-update baseline)
- `(cd crates/iscc-rb && bundle outdated --strict)` — exit 0, "Bundle up to date!"
- `grep 'rb_sys (0\.9\.123)'` and `grep 'rake-compiler-dock (1\.10\.0)'` on Gemfile.lock — both hit;
    hold-back intact
- `grep -c 'held:' crates/iscc-rb/Gemfile` → 2
- `grep 'tag: 0.9.123' .github/workflows/release.yml` — hit; cross-gem image tag still matches
- `grep 'required_ruby_version = ">= 3.1.0"' crates/iscc-rb/iscc-lib.gemspec` — hit; consumer floor
    untouched
- `! grep -qE 'minitest \(6' crates/iscc-rb/Gemfile.lock` — pass; minitest stayed 5.27.0
- `git status --porcelain -- crates/iscc-lib .crap-baseline.json` — empty (no Rust change, no
    baseline refresh owed); `crates/iscc-rb/.standard.yml` also untouched
- `mise run check` — exit 0, all 15 hooks passed, nothing rewritten
- Lockfile invariants held: `PLATFORMS` (`ruby`, `x86_64-linux`) and `BUNDLED WITH 2.6.9` unchanged;
    `rake-compiler` 1.3.1 and `minitest` 5.27.0 already newest allowed, unchanged

**Next:** This was the last fully-locally-verifiable slice of the dependency-refresh issue. The
remaining candidates: **ruff 0.16 adoption** (self-contained, local, retires a documented hold-back
in pyproject.toml) is the best next step; the rest are human-timed or deferred majors (release.yml
action refs, xunit 3.x, Gradle wrapper 9, JUnit 6, magnus 0.8, criterion 0.8, jni 0.22, uniffi
0.32).

**Notes:**

- The resolver moved exactly the set the scoper predicted — no surprises, no new gem releases landed
    mid-step (`bundle outdated --strict` clean after update).
- One lockfile constraint line changed as a side effect of the rubocop bump: rubocop 1.88.2 declares
    `parallel (>= 1.10)` where 1.84.2 declared `parallel (~> 1.10)`. That is upstream metadata
    inside the lock's GEM section, not a dependency we declare — nothing to act on.
- `rake compile` printed a `gmake: Clock skew detected` warning (devcontainer clock vs cached file
    mtimes); build completed fine and tests pass, so it is cosmetic.
- The two HUMAN REVIEW REQUESTED issues (Kotlin consumer floor, Rust-core Unicode divergence) remain
    untouched, per Not In Scope.
